description: testing block
function: testing_function
argument: ['none']
In: ['in']
condition:
- or:
  - Apache > 3.1
  - and:
    - 80 in port
    - or:
      - a == b
      - b == c
Out: ['out']
hint:
- hello

