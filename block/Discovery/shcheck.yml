description: Check security headers on a target website
function: magic_function
argument: ['docker', 'run', '-it', '--rm', 'shcheck', https://<IP>]
In: [IP]
condition: 
Out: []
hint:
  - If you can't execute the cmd above, try cmds below. (open another window)
  - pip3 install shcheck
  - sudo apt install docker.io
  - sudo chmod 666 /var/run/docker.sock
  - docker build -t shcheck .