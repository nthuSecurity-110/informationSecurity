description: check the user have any sudo permission that we can use to escalate
function: magic_function
argument: []
In: []
condition:
Out: []
hint:
  - Run `sudo -l` to see the user's sudo permission.
  - If the allowed command is ALL, enter `sudo bash`
  - If not, check the command out on https://gtfobins.github.io/. and Run the recommended command to escalate.
  - Run `whoami` to check if you get root.
  - If you failed with this user, you can try another user.

