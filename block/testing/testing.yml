description: tetsing
function: testing
argument: ['testing']
In: ['testing']
condition:
- or:
  - Apache > 3.1
  - and:
    - 80 in port
    - or:
      - a == b
      - b == c
Out: ['testing']
hint:
- 'no'

